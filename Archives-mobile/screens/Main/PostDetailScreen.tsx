import React from 'react';
import { ScrollView, Text, StyleSheet } from 'react-native';

export default function PostDetailScreen(){
    return(<ScrollView>
        <Text>Hello. Add post data here</Text>
    </ScrollView>)
}